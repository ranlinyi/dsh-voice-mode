# dsh-voice-mode-adaptation bundle: host voice engine + client voice UI.
# 全局单活语音模式：仅 activeVoiceSession 的 llm/stream 被 tap（朗读），
# 普通会话 next() 直达、零开销（模式隔离，验收点 7）。
- insert:
    - id: voice-mode-adaptation
      name: dsh-voice-mode-adaptation